# FastAPI 服务
fastapi>=0.100.0
uvicorn>=0.22.0

# Windows API（窗口管理、输入模拟）
pywin32>=305

# 视觉处理
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# OCR
easyocr>=1.7.0

# 进程管理（窗口检测用）
psutil>=5.9.0

# 自动化
pyautogui>=0.9.54

# SSE 支持
sse-starlette>=1.6.0
